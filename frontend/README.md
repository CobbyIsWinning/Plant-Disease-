React frontend (Vite) for Plant Disease Prediction demo

How to run locally:

cd frontend
npm install
npm run dev

This will open a dev server (usually http://localhost:5173). The React app posts to http://localhost:8000/predict by default. Update `BACKEND_DEFAULT` in `src/App.jsx` if your API is hosted elsewhere.
